#include <bits/stdc++.h>
using namespace std;
#define fastio ios::sync_with_stdio(false); cin.tie(nullptr);
using ll = long long;

int main() {
    fastio;
    int T;
    if (!(cin >> T)) return 0;
    while (T--) {
        // solve one test case
    }
    return 0;
}